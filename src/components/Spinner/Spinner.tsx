import { LoadingSpinner, LoadingSpinnerContainer } from "./Spinner.style";

const Spinner = () => {
  return (
    <LoadingSpinnerContainer>
      <LoadingSpinner></LoadingSpinner>
    </LoadingSpinnerContainer>
  );
};

export default Spinner;
